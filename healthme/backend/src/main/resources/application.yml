jwt:
  secret: "OF+T6Gn/SXsldpq158IRO+BZER35FWoEPRLAmBq7QWk="

  spring:
    jackson:
      property-naming-strategy: SNAKE_CASE
    main:
      allow-bean-definition-overriding: true
    datasource:
      url: jdbc:mysql://localhost:3306/healthme?serverTimezone=Asia/Seoul
      username: root
      password: your_password
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update  # 초기엔 create 사용해도 됨 (개발용)
      show-sql: true
      properties:
        hibernate:
          format_sql: true
